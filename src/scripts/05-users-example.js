const lightbox = new SimpleLightbox('.gallery a', {
  navText: ['PREV', 'NEXT'],
  captionsData: 'alt',
});

new SimpleLightbox('.gallery a', {
  navText: ['PREV', 'NEXT'],
  captionsData: 'alt',
});

lightbox.refresh();
